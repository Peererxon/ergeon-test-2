type Props = {
    children?: React.ReactNode
};

export const Testing = ({children}:Props) => {
  return (
        
      <>
          <div>testing</div>  
          {children}
      </>
      
  )
}
